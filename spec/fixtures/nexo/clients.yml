# == Schema Information
#
# Table name: nexo_clients
#
#  id         :bigint           not null, primary key
#  service    :integer
#  secret     :string
#  nc_status  :integer
#  brand_name :integer
#  created_at :datetime         not null
#  updated_at :datetime         not null
#
default:
  nc_status: 0
  service: "google"
  secret: <%= ENV.fetch('SEED_GOOGLE_APIS_CLIENT_SECRET') %>
