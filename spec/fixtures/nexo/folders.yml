# == Schema Information
#
# Table name: nexo_folders
#
#  id                  :bigint           not null, primary key
#  integration_id      :bigint           not null
#  nexo_protocol       :integer          not null
#  external_identifier :string
#  name                :string
#  description         :string
#  discarded_at        :datetime
#  created_at          :datetime         not null
#  updated_at          :datetime         not null
#
default:
  name: "Test calendar"
  nexo_protocol: "calendar"
  integration: default
  description: Created automatically with Nexo
  external_identifier: <%= ENV.fetch('TEST_GOOGLE_CALENDAR_ID', "folder-id-default") %>

other:
  name: "Other folder"
  nexo_protocol: "calendar"
  integration: default
  description: Created automatically with Nexo
  external_identifier: <%= ENV.fetch('TEST_GOOGLE_CALENDAR_ID_2', "folder-id-other") %>
